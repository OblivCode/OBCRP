public void SLHandle(data_bundle bundle) {
            string state = "SoloLearn";
            string details = "Loading";
            string title = bundle.title;
            string url = bundle.url;
            int dash_idx = url.IndexOf('/');
            string path = url.Substring(dash_idx);

            if(path.StartsWith("learning/")) {
                //
                int course_id = 
            }

        }
